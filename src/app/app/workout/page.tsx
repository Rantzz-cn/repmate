"use client";
import Image from "next/image";
import Link from "next/link";
import { Check, Minus, Pause, Play, Plus, SkipForward } from "lucide-react";
import { useEffect,useRef,useState } from "react";
import { useRouter } from "next/navigation";
import { useRepMate } from "@/components/providers/app-provider";
import { Button } from "@/components/ui/button";
import { Dialog,DialogContent,DialogDescription,DialogTitle } from "@/components/ui/dialog";
import { ShareRecap } from "@/components/share-recap";
import { exercises } from "@/lib/data";
import type { Workout,WorkoutSet } from "@/lib/types";

export default function WorkoutPage(){const state=useRepMate(),router=useRouter();const workout=state.activeWorkout;const [rest,setRest]=useState(0),[paused,setPaused]=useState(false),[finishOpen,setFinishOpen]=useState(false),[photo,setPhoto]=useState<string|null>(null),[recap,setRecap]=useState<Workout|null>(null);const fileRef=useRef<HTMLInputElement>(null);
useEffect(()=>{if(!rest||paused)return;const timer=setInterval(()=>setRest(v=>Math.max(0,v-1)),1000);return()=>clearInterval(timer)},[rest,paused]);
if(state.loading)return null;if(!workout)return recap?<ShareRecap workout={recap} profile={state.profile} onClose={()=>{setRecap(null);router.replace("/app/progress")}}/>:<div className="grid min-h-[55dvh] place-items-center text-center"><div><h1 className="text-2xl font-semibold">No active workout</h1><p className="mt-2 text-sm text-zinc-500">Start a routine from your program.</p><Link href="/app/programs" className="mt-5 inline-flex min-h-11 items-center rounded-xl bg-white px-5 text-sm font-semibold text-black">Choose a workout</Link></div></div>;
const current=workout.exercises[workout.current],exercise=exercises.find(e=>e.id===current.exerciseId)!;const update=(fn:(w:Workout)=>Workout)=>state.setActive(fn(workout));const updateSet=(index:number,patch:Partial<WorkoutSet>)=>update(w=>({...w,exercises:w.exercises.map((e,i)=>i===w.current?{...e,sets:e.sets.map((s,j)=>j===index?{...s,...patch}:s)}:e)}));const toggleDone=(index:number)=>{const set=current.sets[index];if(!set.done&&(!set.weight||!set.reps))return;updateSet(index,{done:!set.done});if(!set.done){setRest(current.rest);setPaused(false)}};const move=(offset:number)=>{if(offset>0&&current.sets.some(s=>!s.done))return;update(w=>({...w,current:Math.max(0,Math.min(w.exercises.length-1,w.current+offset))}))};const requestFinish=()=>{if(current.sets.some(s=>!s.done))return;setFinishOpen(true)};const choosePhoto=(file?:File)=>{if(!file)return;const reader=new FileReader();reader.onload=()=>setPhoto(String(reader.result));reader.readAsDataURL(file)};const finish=async()=>{const complete={...workout,id:workout.sessionId,completedAt:new Date().toISOString(),duration:Math.round((Date.now()-Date.parse(workout.startedAt))/1000),photo};await state.saveWorkout(complete);await state.setActive(null);setFinishOpen(false);setRecap(complete)};
return <div className="app-page"><header><p className="eyebrow">Active workout · {workout.current+1} of {workout.exercises.length}</p><h1 className="display page-title mt-2">{workout.name}</h1></header><section className="overflow-hidden rounded-3xl border border-white/10 bg-[#111]"><div className="grid h-64 place-items-center bg-white"><Image src={exercise.animation} width={520} height={360} unoptimized alt={`${exercise.name} form`} className="h-full w-full object-contain" onError={event=>{event.currentTarget.src="/assets/images/fallback.webp"}}/></div><div className="p-5"><p className="eyebrow capitalize">{exercise.primaryMuscle} · {exercise.equipment}</p><h2 className="mt-1 text-2xl font-semibold">{exercise.name}</h2><p className="mt-2 text-sm text-zinc-500">Target {current.minReps} to {current.maxReps} reps · {Math.round(current.rest/60)} min rest</p></div></section><section className="rounded-3xl border border-white/10 bg-[#111] p-4"><div className="mb-3 grid grid-cols-[24px_1fr_1fr_48px_40px] gap-2 px-2 text-[9px] font-bold uppercase text-zinc-500"><span>Set</span><span>Weight ({state.profile.units})</span><span>Reps</span><span>Done</span><span/></div><div className="grid gap-2">{current.sets.map((set,index)=><div key={index} className={`grid grid-cols-[24px_1fr_1fr_48px_40px] items-center gap-2 rounded-2xl border p-2 ${set.done?"border-emerald-500/60 bg-emerald-500/5":"border-transparent bg-[#191919]"}`}><span className="numeric text-center text-xs">{index+1}</span><input aria-label={`Set ${index+1} weight`} inputMode="decimal" value={set.weight||""} placeholder="0" onChange={e=>updateSet(index,{weight:Number(e.target.value)})} className="h-12 min-w-0 rounded-xl bg-black px-3 numeric outline-none focus:ring-2 focus:ring-white/30"/><input aria-label={`Set ${index+1} repetitions`} inputMode="numeric" value={set.reps||""} placeholder="0" onChange={e=>updateSet(index,{reps:Number(e.target.value)})} className="h-12 min-w-0 rounded-xl bg-black px-3 numeric outline-none focus:ring-2 focus:ring-white/30"/><button onClick={()=>toggleDone(index)} title={!set.weight||!set.reps?"Enter weight and reps first":"Mark set complete"} className={`grid size-12 place-items-center rounded-xl border ${set.done?"border-emerald-400 bg-emerald-500/50":"border-white/10 bg-black text-zinc-700"}`}><Check className="size-6"/></button><button disabled={current.sets.length<=1} onClick={()=>update(w=>({...w,exercises:w.exercises.map((e,i)=>i===w.current?{...e,sets:e.sets.filter((_,j)=>j!==index)}:e)}))} className="grid size-10 place-items-center rounded-xl border border-white/10 text-zinc-500 disabled:opacity-20"><Minus className="size-4"/></button></div>)}</div><button onClick={()=>update(w=>({...w,exercises:w.exercises.map((e,i)=>i===w.current?{...e,sets:[...e.sets,{weight:0,reps:0,rir:2,done:false}]}:e)}))} className="mt-4 flex min-h-12 w-full items-center justify-center gap-2 rounded-2xl border border-dashed border-white/20 text-sm text-zinc-300"><Plus className="size-4"/>Add set</button>{current.sets.some(s=>!s.done)&&<p className="mt-3 text-xs text-zinc-500">Complete every set before moving to the next exercise.</p>}</section>{rest>0&&<section className="sticky bottom-24 z-40 rounded-3xl border border-white/10 bg-[#181818]/95 p-4 shadow-2xl backdrop-blur-xl"><div className="flex items-center justify-between"><div><p className="eyebrow">Rest timer</p><p className="text-xs text-zinc-500">{exercise.name}</p></div><strong className="numeric text-3xl">{String(Math.floor(rest/60)).padStart(2,"0")}:{String(rest%60).padStart(2,"0")}</strong></div><div className="mt-3 grid grid-cols-4 gap-2"><button onClick={()=>setRest(v=>Math.max(0,v-30))} className="h-10 rounded-xl bg-zinc-900 text-xs">−30</button><button onClick={()=>setPaused(v=>!v)} className="grid h-10 place-items-center rounded-xl bg-zinc-900">{paused?<Play className="size-4"/>:<Pause className="size-4"/>}</button><button onClick={()=>setRest(v=>v+30)} className="h-10 rounded-xl bg-zinc-900 text-xs">+30</button><button onClick={()=>setRest(0)} className="grid h-10 place-items-center rounded-xl bg-zinc-900"><SkipForward className="size-4"/></button></div></section>}<div className="grid grid-cols-[1fr_2fr] gap-2"><Button variant="secondary" disabled={!workout.current} onClick={()=>move(-1)}>Previous</Button>{workout.current===workout.exercises.length-1?<Button disabled={current.sets.some(s=>!s.done)} onClick={requestFinish}>Finish workout</Button>:<Button disabled={current.sets.some(s=>!s.done)} onClick={()=>move(1)}>Next exercise</Button>}</div><Dialog open={finishOpen} onOpenChange={setFinishOpen}><DialogContent className="max-w-sm p-6"><DialogTitle>Save this session</DialogTitle><DialogDescription className="mt-2">Optionally add a photo to remember today&apos;s progress.</DialogDescription><input ref={fileRef} hidden type="file" accept="image/jpeg,image/png,image/webp" onChange={e=>choosePhoto(e.target.files?.[0])}/><button onClick={()=>fileRef.current?.click()} className="mt-5 grid min-h-40 w-full place-items-center overflow-hidden rounded-2xl border border-dashed border-white/20 bg-[#181818]">{photo?<img src={photo} alt="Session preview" className="h-48 w-full object-cover"/>:<span className="text-center"><Plus className="mx-auto mb-2 size-6"/><strong className="text-sm">Add session photo</strong><small className="mt-1 block text-zinc-500">JPG, PNG or WebP</small></span>}</button><div className="mt-5 grid grid-cols-2 gap-2"><Button variant="secondary" onClick={()=>setPhoto(null)}>Skip photo</Button><Button onClick={finish}>Finish workout</Button></div></DialogContent></Dialog>{recap&&<ShareRecap workout={recap} profile={state.profile} onClose={()=>{setRecap(null);router.replace("/app/progress")}}/>}</div>}
