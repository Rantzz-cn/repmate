"use client";
import { ArrowDown, ArrowUp, Search } from "lucide-react";
import { useMemo, useState } from "react";
import { Button } from "./ui/button";
import { Dialog, DialogContent, DialogDescription, DialogTitle } from "./ui/dialog";
import { Input } from "./ui/input";
import { exercises, weekdays } from "@/lib/data";
import type { Program, Routine } from "@/lib/types";
import { titleCase } from "@/lib/utils";

export function RoutineEditor({ program, routine, open, onOpenChange, onSave }: { program: Program; routine: Routine; open: boolean; onOpenChange(value:boolean): void; onSave(program:Program):Promise<void> }) {
  const [weekday,setWeekday]=useState(routine.weekday),[order,setOrder]=useState(routine.exercises.map((item)=>item.exerciseId)),[query,setQuery]=useState(""),[busy,setBusy]=useState(false);
  const filtered=useMemo(()=>{const terms=query.toLowerCase().split(/\s+/).filter(Boolean);return exercises.filter((exercise)=>terms.every((term)=>`${exercise.name} ${exercise.primaryMuscle} ${exercise.equipment}`.toLowerCase().includes(term)));},[query]);
  const toggle=(id:string)=>setOrder((items)=>items.includes(id)?items.filter((item)=>item!==id):[...items,id]);
  const move=(index:number,offset:number)=>setOrder((items)=>{const next=[...items],target=index+offset;if(target<0||target>=next.length)return items;[next[index],next[target]]=[next[target],next[index]];return next;});
  const save=async()=>{setBusy(true);const previous=new Map(routine.exercises.map((item)=>[item.exerciseId,item]));const nextRoutine={...routine,weekday,exercises:order.map((exerciseId)=>previous.get(exerciseId)??{exerciseId,sets:3,minReps:8,maxReps:12}),muscles:[...new Set(order.map((id)=>exercises.find((exercise)=>exercise.id===id)?.primaryMuscle).filter(Boolean))].map((item)=>titleCase(String(item)))};await onSave({...program,days:program.days.map((item)=>item.id===routine.id?nextRoutine:item)});setBusy(false);onOpenChange(false);};
  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent><div className="grid max-h-[92dvh] grid-rows-[auto_auto_minmax(100px,150px)_auto_minmax(120px,1fr)_auto] gap-3 p-5 pt-6"><div className="pr-12"><DialogTitle>Edit {routine.name}</DialogTitle><DialogDescription className="mt-1">Choose exercises and set the exact workout order.</DialogDescription></div><label><span className="mb-1.5 block text-xs font-semibold text-zinc-400">Training day</span><select value={weekday} onChange={(event)=>setWeekday(Number(event.target.value))} className="h-11 w-full rounded-xl border border-white/10 bg-zinc-900 px-3 text-sm">{weekdays.map((day,index)=><option key={day} value={index}>{day}</option>)}</select></label><section className="overflow-y-auto rounded-2xl border border-white/10 bg-black/25 p-3"><header className="mb-2 flex justify-between"><div><strong className="text-sm">Workout order</strong><p className="text-[10px] text-zinc-500">Use the controls to reorder</p></div><span className="rounded-full bg-zinc-900 px-2 py-1 text-[10px] text-zinc-400">{order.length} selected</span></header><div className="grid gap-1.5">{order.map((id,index)=>{const exercise=exercises.find((item)=>item.id===id)!;return <div key={id} className="grid grid-cols-[26px_1fr_auto] items-center gap-2 rounded-xl border border-white/10 bg-zinc-900 p-2"><span className="numeric text-[10px] text-zinc-500">{String(index+1).padStart(2,"0")}</span><span className="truncate text-xs font-semibold">{exercise.name}</span><span className="flex gap-1"><button onClick={()=>move(index,-1)} disabled={!index} className="grid size-8 place-items-center rounded-lg border border-white/10 disabled:opacity-20"><ArrowUp className="size-3.5"/></button><button onClick={()=>move(index,1)} disabled={index===order.length-1} className="grid size-8 place-items-center rounded-lg border border-white/10 disabled:opacity-20"><ArrowDown className="size-3.5"/></button></span></div>})}</div></section><label className="relative"><Search className="absolute left-4 top-1/2 size-4 -translate-y-1/2 text-zinc-500"/><Input value={query} onChange={(event)=>setQuery(event.target.value)} placeholder="Search exercises" className="pl-11"/></label><section className="grid gap-1.5 overflow-y-auto">{filtered.map((exercise)=><label key={exercise.id} className="flex min-h-12 items-center gap-3 rounded-xl border border-white/10 bg-zinc-900 px-3"><input type="checkbox" checked={order.includes(exercise.id)} onChange={()=>toggle(exercise.id)} className="size-4 accent-white"/><span className="min-w-0"><strong className="block truncate text-xs">{exercise.name}</strong><small className="text-[10px] capitalize text-zinc-500">{exercise.primaryMuscle} · {exercise.equipment}</small></span></label>)}</section><footer className="grid grid-cols-2 gap-2 border-t border-white/10 pt-3"><Button variant="secondary" onClick={()=>onOpenChange(false)}>Cancel</Button><Button onClick={save} disabled={busy||!order.length}>{busy?"Saving…":"Save changes"}</Button></footer></div></DialogContent></Dialog>;
}
